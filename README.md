# Khetaan

Khetaan is a WhatsApp-first assistant for Pakistani farmers.

This repository currently contains the initial scaffold for the MVP:

- FastAPI app entrypoint
- agent and scraper package structure
- environment and deployment placeholders
- project log for tracked changes

## Health check

The app exposes `GET /health` and will return a small JSON response when the service is running.
